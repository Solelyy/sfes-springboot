cookie:
  secure: 'false'
  sameSite: Lax

spring:
  datasource:
    username: ${DB_USERNAME}
    url: ${DB_URL}
    password: ${DB_PASSWORD}

jwt:
  secret: ${JWT_SECRET}

logging:
  level:
    org.springframework.boot.context.config: DEBUG